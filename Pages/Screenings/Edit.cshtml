@page
@model Medii_Muresan_brisan2.Pages.Screenings.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Screening</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Screening.ScreeningId" />
            <div class="form-group">
                <label asp-for="Screening.MovieId" class="control-label"></label>
                <select asp-for="Screening.MovieId" class="form-control" asp-items="ViewBag.MovieId"></select>
                <span asp-validation-for="Screening.MovieId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Screening.CinemaId" class="control-label"></label>
                <select asp-for="Screening.CinemaId" class="form-control" asp-items="ViewBag.CinemaId"></select>
                <span asp-validation-for="Screening.CinemaId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Screening.ScreeningTime" class="control-label"></label>
                <input asp-for="Screening.ScreeningTime" class="form-control" />
                <span asp-validation-for="Screening.ScreeningTime" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
